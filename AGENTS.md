# Project Memory

## Python Environment

- This project uses the Conda environment `gaussian-splatting`.
- When running Python commands for this repository, prefer `conda run -n gaussian-splatting python ...`.
